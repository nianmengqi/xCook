# xCook 环境配置

# 服务端口（默认 3000）
PORT=3000

# JWT 密钥（建议生成一个随机密钥）
# 生成命令: openssl rand -hex 64
JWT_SECRET=your-jwt-secret-key

# 管理员密码（用于营养数据管理）
ADMIN_PASSWORD=admin123

# 允许的跨域来源（多个用逗号分隔，可选）
# 例如: https://your-domain.com,https://another-domain.com
CORS_ORIGINS=
